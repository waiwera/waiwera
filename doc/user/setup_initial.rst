******************
Initial conditions
******************

.. can set initial conditions explicitly in JSON input: specify primary, region (constant scalar or array)
.. or restart from output of previous run: specify filename, index
.. in either case, for MINC, can specify if incons contain values for all MINC cells or just single-porosity cells (can extrapolate to MINC matrix cells)

