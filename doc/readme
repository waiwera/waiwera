This directory contains code documentation.

The user/ dubdirectory contains the user guide. To build the user guide, you need to have Sphinx installed (http://www.sphinx-doc.org). In the user/ directory, 'make html' will build the user guide as HTML pages, while 'make latexpdf' will build it as a PDF file.

The ford/ subdirectory contains developer documentation auto-generated by FORD.
Generate dependency graph using: dot depends.dot -Teps -o depends.eps