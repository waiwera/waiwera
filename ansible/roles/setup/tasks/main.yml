---

- name: Create User
  user:
    name: "{{ app_user }}"
    shell: /bin/bash
    comment: waiwera app user
    create_home: yes
  become: yes

- name: Create Group
  group:
    name: "{{ app_group }}"
    # shell: /bin/bash
    # comment: waiwera app user
  become: yes

- name: Load Variables
  include: ../../common/tasks/load_vars.yml

- name: Setup packages
  include: ../../common/tasks/packages.yml
  become: yes

